# 业码汇 v0.1.19 数据测试报告

## 测试结论

本轮测试通过。主项目后端集成测试 17/17 通过，独立授权中心测试 4/4 通过，前端 TypeScript 检查和 Vite 生产构建通过。

## 测试环境

| 项目 | 结果 |
| --- | --- |
| 操作系统 | macOS（用于源码和接口测试） |
| Python | 3.12 虚拟环境 |
| 前端构建 | Node.js 22.19.0、Vite 5.4.21 |
| 主项目版本 | v0.1.19 |
| 授权中心版本 | v0.1.0 |
| 数据库 | 隔离 SQLite 临时库 |

## 主项目覆盖范围

- 首次启用、管理员登录、权限角色和本机安装限制。
- 编号年度、事务所、业务类型、编号规则及 `{yyyy}`、`{yy}`、序号占位符校验。
- 2026 编号年度下建立 2024、2025 两个业务年度项目，验证编号依次使用 `2026-0001`、`2026-0002`，不按业务年度编号。
- 项目分配、执业人员签字人绑定、重名人员账号区分及签字项目查询。
- 发票和收款逐笔登记、汇总、余额和导出。
- 编号发放、回收、历史追踪及格式冲突保护。
- 数据库迁移、旧版汇总数据迁移、签字人历史数据保留。
- Windows 管理工具核心逻辑：服务健康检查、配置地址、安装包校验、数据库备份、升级回滚和路径安全。
- 升级过程拒绝降级、拒绝错误版本包，升级失败恢复程序和数据库。
- 客户端授权状态：有效、暂停、撤销授权文件均按预期处理；未启用授权时保持开发模式兼容。

## 授权中心覆盖范围

- 管理员登录、客户创建、授权签发和 Ed25519 签名校验。
- 实例数量上限、用户数量上限、版本范围和离线宽限期。
- 授权续期、暂停、恢复、撤销和实例解绑。
- 心跳上报、审计记录及签名篡改拒绝。

## 构建验证

- `python -m unittest discover -s backend/tests -p 'test_*.py' -v`：17/17 通过。
- `python -m unittest discover -s license-server/tests -p 'test_*.py' -v`：4/4 通过。
- `npm run build`：通过，生成 `frontend/dist`。

Windows EXE 和 Windows 服务需要在 Windows Server 或 Windows 环境进行最终安装和启动烟测；当前 macOS 环境只完成了打包脚本、包校验、升级回滚和服务管理核心逻辑测试。
